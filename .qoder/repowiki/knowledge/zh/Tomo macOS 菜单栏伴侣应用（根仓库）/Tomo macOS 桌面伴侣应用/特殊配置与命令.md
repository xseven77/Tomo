构建与打包通过 bash 脚本完成：`./package_app.sh` 执行 swift build -c release、自动清理模块缓存、codesign 签名、生成 ZIP 与 UDZO 压缩的 DMG；`./rebuild_and_run.sh` 用于开发调试；`./release_app.sh` 用于发布流程。CLI 模式可通过 `--probe-chatgpt-apis` 参数运行 API 探测。