- README 与 PROJECT.md 保持中文说明与英文技术术语并存，截图路径统一放在 `assets/screenshots/`。
- 本地数据一律写入 `~/Library/Application Support/Tomo/`，OAuth token 权限固定为 `0600`。
- 对外 API（OpenAI OAuth、GitHub Releases）通过本地回调端口（1455）或标准浏览器完成，不直接处理敏感凭据。
- 对 Codex 本地数据的访问采用只读解析（SQLite 索引 + JSONL rollout），仅用户显式操作时才写入 Pet 配置。